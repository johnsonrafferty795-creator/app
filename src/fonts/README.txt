Grenze Gotisch, a variable weight font, used for the gothic theme only.
One file covers the whole 100-900 range, so the @font-face declares
font-weight: 100 900 and the browser interpolates.

Licensed under the SIL Open Font License 1.1 — see OFL.txt.
Fetched from Google Fonts (latin subset).

Anton, used for the Winter Arc display face — one weight, heavy and
condensed, the way a gym poster is set. Latin subset only.

Both are licensed under the SIL Open Font License 1.1 (OFL.txt) and were
fetched from Google Fonts.
